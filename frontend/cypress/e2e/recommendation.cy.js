describe("recommendation tests", () => {
  beforeEach(() => {
    cy.visit("/");
  });
  it("run recommendation function correctly", () => {
    // due to the limitation of time, I can't write more tests
  });
});