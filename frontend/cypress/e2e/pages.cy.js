describe("Pages tests", () => {
  beforeEach(() => {
    cy.visit("/");
  });
  it("run pages function correctly", () => {
    // due to the limitation of time, I can't write more tests for pages
  });
});