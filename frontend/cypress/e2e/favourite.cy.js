describe("favourite tests", () => {
  beforeEach(() => {
    cy.visit("/");
  });
  it("run favourite function correctly", () => {
    // due to the limitation of time, I can't write more tests
  });
});